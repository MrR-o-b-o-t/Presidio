{
  "engines": {
    "node": "16.15.0",
    "npm": "8.19.1"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "scripts": {
    "start": "npm run start --prefix server",
    "heroku-prebuild": "npm i && cd client && npm i && npm run build"
  }
}